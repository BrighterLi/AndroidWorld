<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xiaoming.view">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".touchevent3.TouchEvent3Activity"></activity>
        <activity android:name=".touchevnet2.TouchEvent2Activity" />
        <activity android:name=".relativelayout.TestRelativeLayoutActivity" />
        <activity android:name=".onmeasure.OnMeasureGroupViewActivity" />
        <activity android:name=".touchevent.TouchActivity" />
        <activity android:name=".ViewActivity" />
        <activity android:name=".onmeasure.OnMeasureViewActivity" />
        <activity android:name=".onmeasure.OnMeasureGroupView2Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>