<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xiaoming.a002netretrofit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".upload.UploadActivity"></activity>
        <activity android:name=".get3.Get3Activity" />
        <activity android:name=".post2.Post2Activity" />
        <activity android:name=".asyncget2.AsyncGet2Activity" />
        <activity android:name=".synget.SynGetActivity" />
        <activity android:name=".post.PostActivity" />
        <activity android:name=".asynget.GetActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>